apiVersion: v1
kind: Pod
spec:
  containers:
    - name: app
      image: app
      env:
        - name: PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: password
        - name: ENDPOINT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: endpoint
